<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.naver.jihyunboard.board.dao.BoardDAO">
	
<!-- 	<resultMap type="BoardDAO" id="result">
		<result property="boardContent" column="boardContent"/>
		<result property="boardUserId" column="boardUserId"/>
		<result property="boardTitle" column="boardTitle"/>
		<result property="boardContent" column="boardContent"/>
		<result property="boardCategory" column="boardCategory"/>
		<result property="boardDate" column="boardDate"/>
		<result property="boardReadCount" column="boardReadCount"/>
	</resultMap>
 -->
    <!-- 게시글 전체 목록 조회 -->
    <select id="listAll" resultType="com.naver.jihyunboard.board.dto.BoardDTO">
      SELECT * FROM board
    </select>
    
    <!-- 새 글 등록  -->
     <insert id="insert" parameterType="com.naver.jihyunboard.board.dto.BoardDTO">
        INSERT INTO board (boardTitle, boardContent, boardCategory)
        VALUES (#{boardTitle},#{boardContent},#{boardCategory} )
    </insert>
    
    <!-- 선택한 게시글 보기  -->
     <select id="viewBoard" resultType="com.naver.jihyunboard.board.dto.BoardDTO">
     	SELECT boardUserid, boardTitle, boardContent, boardCategory, boardDate
     	FROM board
     	WHERE boardNum = #{boardNum} 
     </select>

</mapper>